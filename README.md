# Яндекс Практикум "Продвинутый Go Разработчик" 🚀  
## Сервис сбора метрик и алертинга 📊

## 📌 Описание проекта

Это система для сбора runtime-метрик, разработанная в рамках трека **«Сервис сбора метрик и алертинга»** курса **«Продвинутый Go-разработчик»** от Яндекс Практикума.  
Состоит из двух компонентов:

- **Сервер** — принимает и хранит метрики;
- **Агент** — собирает метрики из среды выполнения и отправляет их на сервер.

---

## ⚙️ Метрики

Поддерживаются два типа метрик:

- **gauge** (`float64`) — значение перезаписывается при каждом обновлении;
- **counter** (`int64`) — значение увеличивается на заданное при каждом обновлении.

---

## 🌐 Сервер

Сервер слушает `http://localhost:8080` и обрабатывает http запросы  

---

## 🛰 Агент

Агент предназначен для автоматического сбора метрик из стандартной библиотеки `runtime` и их периодической отправки на сервер.

### Что делает агент:

- Использует `runtime.ReadMemStats` для сбора метрик (`Alloc`, `TotalAlloc`, `Sys`, и др.);
- Обновляет значения `counter` метрик (например, количество попыток отправки);
- Отправляет данные на сервер с заданной периодичностью (`pollInterval`, `reportInterval`);
- Работает параллельно, используя `context.Context` и фоновые воркеры.

Агент можно запустить отдельно, указав адрес сервера и частоту опроса/отправки метрик через конфигурацию.

---

## 🏗 Структура проекта

Проект имеет слоистую архитектуру, взаимодействие слоев осуществляется с помощью интерфейсов.

| Слой         | Назначение                                                                                      |
|--------------|------------------------------------------------------------------------------------------------|
| apps         | Сборка и инициализация приложения: создание и настройка HTTP сервера, маршрутов, запуск серверов |
| configs      | Конфигурация приложения: структуры настроек, опции и обработка параметров                        |
| handlers     | HTTP-хендлеры: обработка входящих HTTP-запросов, реализация API для метрик                      |
| routers      | Настройка маршрутизации HTTP-запросов, связывание путей с обработчиками                         |
| engines      | Слой работы с сырыми данными                         |
| repositories | Слой доступа к данным: хранение и извлечение метрик (в памяти, БД и др.)                         |
| services     | Бизнес-логика приложения: обработка и обновление метрик, применение правил и вычислений         |
| types        | Определение основных типов данных, структур и констант                                         |
| validators   | Валидация входных данных и проверка корректности метрик                                        |
| runners      | Управление жизненным циклом приложения: запуск HTTP сервера, обработка сигналов ОС, управление воркерами |
| facades      | Фасады для выполнения  HTTP-запросов                                                             |
| workers      | Воркеры                                                                                          


```
.
├── cmd
│   ├── agent
│   │   ├── agent              # Основной код агента сбора метрик
│   │   ├── flags.go          # Парсинг и обработка флагов командной строки агента
│   │   ├── flags_test.go     # Тесты для флагов агента
│   │   ├── main.go           # Точка входа для запуска агента
│   │   ├── main_test.go      # Тесты для main.go агента
│   │   ├── run.go            # Логика запуска и управления агентом
│   │   └── run_test.go       # Тесты для run.go агента
│   └── server
│       ├── flags.go          # Парсинг и обработка флагов командной строки сервера
│       ├── flags_test.go     # Тесты для флагов сервера
│       ├── main.go           # Точка входа для запуска сервера
│       ├── main_test.go      # Тесты для main.go сервера
│       ├── run.go            # Логика запуска и управления сервером
│       ├── run_test.go       # Тесты для run.go сервера
│       └── server            # Реализация самого сервера (http handlers, роутинг и пр.)
├── go.mod                   # Модуль Go: зависимости и настройки проекта
├── go.sum                   # Контрольные суммы зависимостей для go.mod
├── internal
│   ├── apps
│   │   ├── agent.go          # Инициализация и конфигурация приложения агента
│   │   ├── agent_test.go     # Тесты для кода агента в apps
│   │   ├── server.go         # Инициализация и конфигурация приложения сервера
│   │   └── server_test.go    # Тесты для кода сервера в apps
│   ├── configs
│   │   ├── agent.go          # Структуры и логика конфигурации агента
│   │   ├── agent_test.go     # Тесты конфигурации агента
│   │   ├── server.go         # Структуры и логика конфигурации сервера
│   │   └── server_test.go    # Тесты конфигурации сервера
│   ├── engines
│   │   ├── memory_storage.go # Хранилище метрик в памяти
│   │   └── memory_storage_test.go # Тесты для memory_storage.go
│   ├── errors
│   │   ├── common.go         # Общие ошибки проекта
│   │   └── metrics.go        # Ошибки, связанные с метриками
│   ├── facades
│   │   ├── metric_update.go  # Фасад для обновления метрик (интерфейс между слоями)
│   │   └── metric_update_test.go # Тесты фасада обновления метрик
│   ├── handlers
│   │   ├── helpers.go        # Вспомогательные функции для HTTP-хендлеров
│   │   ├── metric_get_path.go # Обработка запросов на получение метрик по пути
│   │   ├── metric_get_path_mock.go # Мок для metric_get_path.go (тестирование)
│   │   ├── metric_get_path_test.go # Тесты для metric_get_path.go
│   │   ├── metric_html_test.go # Тесты для HTML представления метрик
│   │   ├── metric_list_html.go # Генерация HTML списка метрик
│   │   ├── metric_list_html_mock.go # Мок для metric_list_html.go
│   │   ├── metric_update_path.go # Обработка запросов обновления метрик по пути
│   │   ├── metric_update_path_mock.go # Мок для metric_update_path.go
│   │   └── metric_update_path_test.go # Тесты для metric_update_path.go
│   ├── logger
│   │   ├── logger.go         # Инициализация и настройка логгера
│   │   └── logger_test.go    # Тесты для логгера
│   ├── repositories
│   │   ├── metric_memory_get.go  # Получение метрик из памяти (репозиторий)
│   │   ├── metric_memory_get_test.go # Тесты для metric_memory_get.go
│   │   ├── metric_memory_list.go # Листинг метрик из памяти
│   │   ├── metric_memory_list_test.go # Тесты для metric_memory_list.go
│   │   ├── metric_memory_save.go # Сохранение метрик в память
│   │   └── metric_memory_save_test.go # Тесты для metric_memory_save.go
│   ├── routers
│   │   ├── metrics.go        # HTTP роуты для метрик (маршрутизация)
│   │   └── metrics_test.go   # Тесты роутеров
│   ├── runners
│   │   ├── context.go        # Контекст и управление жизненным циклом приложения
│   │   ├── context_test.go   # Тесты для контекста
│   │   ├── http_server.go    # Запуск HTTP сервера
│   │   ├── http_server_mock.go # Мок для http_server.go
│   │   ├── http_server_test.go # Тесты для HTTP сервера
│   │   ├── worker.go         # Рабочие процессы/горутины
│   │   └── worker_test.go    # Тесты для worker.go
│   ├── services
│   │   ├── metric_get.go     # Бизнес-логика получения метрик
│   │   ├── metric_get_mock.go # Мок для metric_get.go
│   │   ├── metric_get_test.go # Тесты для metric_get.go
│   │   ├── metric_list.go    # Бизнес-логика листинга метрик
│   │   ├── metric_list_mock.go # Мок для metric_list.go
│   │   ├── metric_list_test.go # Тесты для metric_list.go
│   │   ├── metric_update.go  # Бизнес-логика обновления метрик
│   │   ├── metric_update_mock.go # Мок для metric_update.go
│   │   └── metric_update_test.go # Тесты для metric_update.go
│   ├── types
│   │   ├── api_error.go      # Определения ошибок API
│   │   ├── metrics.go        # Типы и структуры для метрик
│   │   └── metrics_test.go   # Тесты для типов метрик
│   ├── validators
│   │   ├── metrics.go        # Валидация метрик (проверка корректности)
│   │   └── metrics_test.go   # Тесты для валидации метрик
│   └── workers
│       ├── metric_agent.go   # Логика работы агента метрик
│       ├── metric_agent_mock.go # Мок для metric_agent.go
│       └── metric_agent_test.go # Тесты для metric_agent.go
├── Makefile                 # Скрипты сборки и тестирования проекта
└── README.md                # Документация и описание проекта
```

## 🛠 Используемые технологии

| Технология / Библиотека | Описание                            |
|-------------------------|-------------------------------------|
| Go                   | Язык программирования               |
| Chi                  | HTTP роутер                         |
| Sqlx                 | Драйвер БД (Postgres)               |
| Resty                | HTTP клиент                         |
| Testify              | Фреймворк для тестирования          |
| Docker               | Утилита для контейнеризации         |

---

## 🔄 Инкременты проекта

| Итерация | Описание                                               |
|----------|--------------------------------------------------------|
| iter1    | Реализация сервера метрик                              | 
| iter2    | Реализация агента сбора метрик(опрос и отрпавка runtime метрик на сервер) | 
| iter3    | Добавлены обработчики для получения значений метрик | 